import styled from 'styled-components';

export const Container = styled.div`
  background: var(--secondary);
  border-radius: 16px;

  width: 100%;
  padding: 1rem;

  h2.title {
    font-weight: bold;
    font-size: 19px;
  }
`;
